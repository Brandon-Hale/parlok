# parlok (Python SDK)

Status: v0 skeleton. Public interfaces are defined; the policy engine and adapters land in v0.1.

## Install (editable, for dev)

```bash
pip install -e ".[dev]"
```

## Test

```bash
pytest
```
